# Segurança mínima (MVP)

- Não versionar segredos no repositório
- Usar variáveis de ambiente para tokens/chaves
- Minimizar escopos OAuth Google
- Mascarar IDs sensíveis e tokens em logs
- Definir rotação de segredo para deploy público
- Validar origem de webhook Telegram quando integrar webhook
